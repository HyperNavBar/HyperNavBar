# 适配教程

本文档面向希望为 HyperNavBar 贡献第三方应用适配规则的开发者，提供从获取应用到提交规则的全流程指导。

<br>

## 阅前提要

由于维护者精力有限，不会主动适配不常用或者自己不用的软件。

提交 Issue 时请给出应用内截图、应用包名、必需的活动适配信息和具体适配要求。
Pull Request 前建议在本地进行验证，提交时确保已填写更新日志。

<br>

## 规则格式说明

### 配置文件基本结构

```json5
{
  "name": "规则名称（可选）",
  "dataVersion": "260630",
  "modules": "navigation_bar_immersive_application_config_new",
  "modifyApps": "modifyApps",
  "NBIRules": {
    "应用包名": {
      "name": "应用名称（可选）",
      "enable": true,
      "activityRules": {
        "活动名称": {
          "mode": "适配模式",
          "color": "颜色值"
          //"sf_sampling_mode": "SF采样模式",
          //"dialogMode": "对话框模式",
          //"popupMode": "弹窗模式",
          //"appNavColorDisabled": "禁用应用导航栏颜色"
        }
      }
    }
  }
}
```

### 应用级别配置选项

| 字段名                    | 类型      | 可选值             | 默认值     | 说明                |
|------------------------|---------|-----------------|---------|-------------------|
| **name**               | string  | 任意字符串           | -       | 应用显示名称（仅用于方便管理） |
| **enable**             | boolean | `true`, `false` | `false` | 基础启用标志            |
| **disableVersionCode** | long    | 数字或 `null`      | `null`  | 当应用版本号小于等于此值时禁用规则 |

**启用逻辑：** `最终启用状态 = enable OR enable31`，只要有一个为 true 就启用，两个都为 false 才禁用。实际使用时建议只使用 `enable`。

### 活动级别配置选项

活动名称填写完整字符串，可以使用 `*` 作为通配符。如 `*` 指代全部活动，`com.tencent.mm.plugin.appbrand.ui.AppBrandUI*` 指代所有微信小程序活动。

#### mode（沉浸模式）

| 值            | 名称   | 说明                       |
|--------------|------|--------------------------|
| **-1 (255)** | 自动模式 | 未配置规则，由系统算法自动检测适配        |
| **0**        | 禁用模式 | 明确禁用自动沉浸，保持应用原有样式        |
| **1**        | 视图取色模式 | 启用沉浸，使用 color 字段或视图采样    |
| **2**        | 悬浮模式 | 通过悬浮隐藏导航栏背景（完全沉浸，但会遮挡内容） |

#### color（导航栏颜色）

> 可以在 color 项填写常见的 16 进制 RGBA 格式或 RGB 格式字符串，构建时会自动转换格式

| 类型              | 示例值         | 对应颜色            | 说明           |
|-----------------|-------------|-----------------|--------------|
| **特殊值**         | `1`         | -               | 系统自动检测底部颜色   |
| **ARGB 32位整数值** | `-16777216` | 黑色 (0xFF000000) | 直接设置导航栏为指定颜色 |
|                 | `-1`        | 白色 (0xFFFFFFFF) | 直接设置导航栏为指定颜色 |
| **未设置或 null**   | -           | -               | 使用默认逻辑       |

#### sf_sampling_mode（SF 采样模式）

| 值       | 名称       | 说明                     |
|---------|----------|------------------------|
| **0**   | 自动模式（默认） | 由系统自动判断是否需要启用SF采样      |
| **1**   | 强制启用模式   | 强制启用SurfaceFlinger屏幕采样 |
| **255** | 强制禁用模式   | 强制禁用SF采样               |

#### dialogMode（对话框模式）

| 值     | 名称         | 说明                 |
|-------|------------|--------------------|
| **0** | 禁用模式       | 对话框窗口禁用沉浸适配        |
| **1** | 视图采样模式（默认） | 对话框启用沉浸，使用视图采样取色   |
| **2** | SF 采样模式    | 对话框启用沉浸，使用 SF 采样取色 |

#### popupMode（弹窗模式）

| 值     | 名称         | 说明              |
|-------|------------|-----------------|
| **0** | 禁用模式       | 弹窗窗口禁用沉浸适配      |
| **1** | 视图采样模式（默认） | 弹窗启用沉浸，使用视图采样取色 |
| **2** | SF 采样模式     | 弹窗启用沉浸，使用SF采样取色 |

#### appNavColorDisabled（禁用应用导航栏颜色）

仅 mode=1 时生效。

| 值     | 名称      | 说明              |
|-------|---------|-----------------|
| **0** | 不禁用（默认） | 允许该应用的自动导航栏颜色适配 |
| **1** | 禁用      | 禁用该应用的自动导航栏颜色适配 |

### 优先级说明

**启用优先级：** 应用级别 enable/enable31 > 活动级别 mode > 具体规则

**颜色来源优先级（mode=1 时）：** color 字段指定（color≠1）> SF采样（sf_sampling_mode=1）> 视图采样（color=1）> 系统默认

**规则匹配优先级：** 具体活动名称 > 通配符 `*` 规则

**特殊情况：** 若同时配置 `mode=0`（禁用）和 `sf_sampling_mode=1`，SF采样优先。`dialogMode` 和 `popupMode` 仅对相应类型窗口生效。

### 推荐适配策略

#### 模式选择指南

- **mode=1（视图取色模式）**：有固定底栏的页面、颜色相对固定的界面、需要精确颜色匹配的场景
- **mode=2（悬浮模式）**：信息流页面（微博、知乎等）、搜索/设置等无底部固定控件的页面、需最大化显示面积的场景
- **mode=0（禁用模式）**：应用自身已有良好适配、有兼容性问题的页面、视频播放等全屏场景
- **mode=-1（自动模式）**：不确定最佳适配方式的页面，作为默认回退方案

#### SF 采样模式选择

- **普通界面**：使用默认值 `0`
- **底部颜色接近纯色，变化频率小**（如不同皮肤不同颜色）：使用 `1`
- **底部颜色复杂且快速变化或有兼容性问题**（如图片预览、信息流界面）：使用 `255`

<br>

## 工作原理

HyperNavBar 通过替换系统内的导航栏沉浸规则配置文件来实现优化。

应用需要**同时订阅两个规则源**：

- **官方规则源**（系统原始配置）— 提供系统默认的沉浸规则，作为基础
- **社区规则源**（由 HyperNavBar Rules 仓库维护）— 只包含与官方不同的自定义规则，用于补充和修正官方规则的不足

社区规则源在订阅列表中位于官方规则源上方，合并时优先生效。发版时会自动移除与官方重复的活动规则，因此社区规则源只保留自定义规则。

配置文件为 JSON 格式，支持灵活的沉浸式规则配置。

<br>

## 如何反馈问题

当你在使用规则时，若遇到未能适配或适配失效的应用场景，欢迎随时向我们提供反馈。

为了帮助我们更高效地解决问题，建议你提供尽可能详细的信息：

- **应用名称** — 应用的显示名称
- **应用包名** — 应用的唯一标识（package name）
- **活动信息** — 需要适配的对应页面的活动名称
- **截图** — 需要适配的对应页面的截图

### 获取应用包名和活动信息

打开 HyperNavBar，进入可视化编辑，点击「悬浮窗识别」按钮，切换到目标应用，即可通过悬浮窗查看界面信息，点击「一键添加」即可设置该界面的适配方式。

> 也可通过 ADB 命令 `adb shell dumpsys activity activities` 或第三方工具（如 MT 管理器）获取。

<br>

## 提交适配

### 准备工作

1. 安装并测试应用的各种页面
2. 确定需要适配的应用包名和活动名称
3. 选择合适的适配模式组合
4. 如需取色，确定合适的颜色值

### 修改配置文件

1. 在 `rules/immerse_rules.json` 中添加或修改规则
2. 在 `changelog.md` 中添加更新说明

### 验证要求

- 在本地设备上测试所有适配页面的效果
- 确保不会造成界面异常或功能问题
- 测试对话框和弹窗场景
- 截图记录适配前后的对比

<br>

## 常见问题

### 配置为什么不生效？

1. 应用强制设置了导航栏样式（使用 `appNavColorDisabled: 1` 尝试解决）
2. 活动名称不正确
3. 被系统云控覆盖
4. 配置文件格式错误
5. 应用版本号未超过 `disableVersionCode`

### 通配符 `*` 会覆盖具体活动规则吗？

不会。具体活动规则优先级高于通配符规则。系统会优先匹配具体的活动名称，未匹配到时才使用通配符规则。

### `color=1` 和其他颜色值有什么区别？

- `color: 1`：触发自动视图采样，系统分析界面底部颜色
- `color: 其他整数值`：直接使用该颜色值，不进行采样
- 不设置 color 字段：使用默认逻辑

### SF 采样和视图采样有什么区别？

- **视图采样**：分析应用视图树的颜色，精度高但性能开销大
- **SF 采样**：直接从屏幕像素采样，性能好但可能不够精确，且略有延迟
- 复杂界面推荐使用 SF 采样（`sf_sampling_mode: 1`）

<br>

## 重要提示

- 适配前请充分测试各种场景（正常界面、对话框、弹窗）
- 记录所有修改和测试结果
- 对于复杂的应用，建议分步骤适配
- 如果遇到无法解决的问题，可以先使用 `mode: -1` 让系统自动处理
- 提交 PR 时请确保配置格式正确，所有字段值有效
